---
applications:
  .:
    name: remote-presentation-controller
    framework:
      name: node
      info:
        mem: 64M
        description: Node.js Application
        exec: 
    infra: ap-aws
    url: ${name}.${target-base}
    mem: 64M
    instances: 1
